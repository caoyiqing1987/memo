# CLAUDE.md for memo project

## Memory System

This project itself uses `.memo/` for persistent memory.
- Read `.memo/INDEX.md` at session start
- Load relevant entries from `.memo/entries/` as needed
- Save new information with `memo add "<text>"`
